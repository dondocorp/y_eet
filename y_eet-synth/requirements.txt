# Core HTTP and async
aiohttp>=3.9.0,<4.0
# CLI
click>=8.1.0,<9.0
# Config
pyyaml>=6.0
python-dotenv>=1.0.0
# Payload generation
faker>=24.0.0
# Terminal output
rich>=13.0.0
# OpenTelemetry — API + SDK
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
# OTel propagators
opentelemetry-propagator-b3>=1.21.0
# OTel OTLP exporter (gRPC)
opentelemetry-exporter-otlp-proto-grpc>=1.21.0
# OTel auto-instrumentation for aiohttp (optional but useful in CI)
opentelemetry-instrumentation-aiohttp-client>=0.42b0
